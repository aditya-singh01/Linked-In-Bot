# Core (required to run the bot)
undetected-chromedriver>=3.5.0
pyautogui>=0.9.54
setuptools>=65.0.0
selenium>=4.0.0

# Web UI (Applied Jobs history)
flask>=3.0.0
flask-cors>=4.0.0

# Optional: AI features (uncomment if using use_AI = True)
# openai>=1.0.0
# google-generativeai>=0.3.0

# Optional: Resume generator (uncomment if using)
# python-docx>=1.0.0
# fpdf2>=2.7.0
